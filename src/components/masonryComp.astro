---
import projectList from '../data/projectList.json';
// import logoImg from "@/assets/fz-logo.png";
---

<!-- Masonry grid container -->
<div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
    {projectList.map(project => (
        <div class="group relative cursor-pointer rounded-md overflow-hidden">
            <img src={project.image} alt={project.number} class="w-full object-cover rounded-md shadow-lg opacity-50" />
            <div class="absolute inset-0 bg-black bg-opacity-50 flex items-center justify-center opacity-75 group-hover:opacity-100 transition-opacity duration-300">
                <p class="text-white text-lg pl-5">{project.title}</p>
            </div>
        </div>
    ))}
</div>