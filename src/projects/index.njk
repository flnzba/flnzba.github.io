---
layout: layouts/base.njk
title: Projects
permalink: /projects/
eleventyExcludeFromCollections: true
---
{% from "partials/page-header.njk" import pageHeader %}

{{ pageHeader("Work", "Projects", "Client work, written up. Each one names the problem, the stack and what actually changed once it ran.") }}

<section class="u-shell py-14">
  <ol class="space-y-px">
    {% for project in collections.projects %}
      {% set rowIndex = loop.index %}
      {% include "partials/numbered-row.njk" %}
    {% endfor %}
  </ol>
</section>
