#!/bin/sh
# harness:generated - managed by .harness/.agents/scripts/install.mjs; overwritten on update.
set -e
[ -f ".harness/Makefile" ] || git submodule update --init ".harness"
[ -d ".harness/.agents/vendor/mattpocock-skills/skills" ] || ( unset GIT_DIR GIT_INDEX_FILE GIT_WORK_TREE GIT_PREFIX; git -C ".harness" submodule update --init ".agents/vendor/mattpocock-skills" )
[ -d ".harness/.agents/vendor/impeccable/.agents/skills" ] || ( unset GIT_DIR GIT_INDEX_FILE GIT_WORK_TREE GIT_PREFIX; git -C ".harness" submodule update --init ".agents/vendor/impeccable" && { git -C ".harness/.agents/vendor/impeccable" sparse-checkout set .agents/skills/impeccable 2>/dev/null || true; } )
make -f ".harness/Makefile" harness-check-imported
